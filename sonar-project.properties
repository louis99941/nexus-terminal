# SonarCloud 配置
sonar.projectKey=Silentely_nexus-terminal
sonar.organization=silentely

# 排除国际化翻译文件，避免 JSON key 结构相似导致重复率误报
sonar.exclusions=**/locales/**/*.json

# 排除生成文件和依赖
sonar.exclusions+=**/node_modules/**,**/dist/**,**/build/**,**/*.test.ts,**/*.spec.ts
